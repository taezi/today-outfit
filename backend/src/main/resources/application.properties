spring.application.name=backend
spring.config.import=optional:file:.env[.properties]

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/today_outfit}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

weather.kma.base-url=https://apis.data.go.kr/1360000/VilageFcstInfoService_2.0
weather.kma.service-key=${KMA_SERVICE_KEY:}
weather.kma.nx=60
weather.kma.ny=127
weather.kma.location-name=\uC11C\uC6B8 \uC885\uB85C\uAD6C

recommendation.mode=${RECOMMENDATION_MODE:temporary}
recommendation.codex-cli.command=${CODEX_CLI_COMMAND:codex}
recommendation.codex-cli.working-directory=${CODEX_CLI_WORKING_DIRECTORY:..}
recommendation.codex-cli.model=${CODEX_CLI_MODEL:}
recommendation.codex-cli.timeout-seconds=${CODEX_CLI_TIMEOUT_SECONDS:60}
